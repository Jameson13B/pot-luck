import React from "react";
import styled from "styled-components";

const Home = props => {
  return (
    <HomeContainer>
      <h1>Home</h1>
    </HomeContainer>
  );
};

export default Home;

const HomeContainer = styled.div``;
